#include <iostream>
#include <vector>
#include "testlib.h"

using namespace std;



void input() {
  /*
  N = inf.readInt(MIN_N, MAX_N, "N");
  inf.readEoln();
  for(int i = 0; i < N; i++) {
    a[i] = inf.readInt(MIN_A, MAX_A, format("a[%d]", i+1));
    inf.readEoln();
  }
  inf.readEof();
  */
}

void check() {
}

int main() {
  registerValidation();
  input();
  check();
  return 0;
}
